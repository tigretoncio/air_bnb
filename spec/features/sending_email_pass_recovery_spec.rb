feature "sending emails" do

  # let(:user) { User.first }

  scenario 'it calls the SendRecoverLink service to send the link' do
    # expect(SendRecoverLink).to receive(:call)
    # request_reset_password
  end
end